# Contract

The registry under this directory is intentionally small and stable. A scenario file describes workload identity and observation categories, not implementation strategy. Consumers may add derived evidence artifacts elsewhere, but must not mutate the scenario fixture to make a candidate look better.
