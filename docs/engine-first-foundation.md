# First foundation state

`feat/maps-engine-foundation` is the first implementation branch under Milestone A. It introduces real `maps-core` camera/projection/tile types and tests together with canonical scenario identity. It must be integrated only after these types are wired into the crate exports and normal verification so they cannot become dead scaffolding.
