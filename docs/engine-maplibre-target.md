# MapLibre-class target

Maps becomes a practical MapLibre alternative only after the first-party runtime can consume vector tiles, evaluate the adopted style semantics, prepare/render vector geometry and place stable labels/symbols without MapLibre runtime ownership. WebGPU alone, or independent point layers over a MapLibre camera, does not meet this target.
