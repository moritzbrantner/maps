# Initial acceptance evidence

The first engine foundation PR is accepted on existing exact-head Rust/fast/browser/package checks plus deterministic scenario-registry validation. It must not add a fake browser-profiler gate before runtime-profiler's browser journey capability exists. Missing future profiler support remains an explicit Milestone A gap, not a passing check.
