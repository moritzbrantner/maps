# Engine milestone definition of done

A milestone is complete only when its subsystem is useful end to end, its semantic owner is unambiguous, canonical scenario evidence exists, hosted exact-head acceptance is green, required fallback behavior is explicit, and superseded architecture has been removed or reduced to an intentional reference/fallback boundary. Scaffolding, unused contracts, demo-only behavior, or green microbenchmarks alone do not complete a milestone.
