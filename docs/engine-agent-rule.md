# Agent implementation rule

Agents should optimize for milestone subsystem convergence, not issue-count throughput. A reviewable PR may be narrow enough to validate safely, but it should connect to the running subsystem and leave behind reusable contracts/evidence. Avoid unused scaffolding PR chains whose only value depends on many future micro-slices.
