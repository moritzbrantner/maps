# runtime-profiler integration

Maps declares representative canonical scenarios. runtime-profiler executes/captures supported scenario journeys and emits immutable validated bundles plus neutral evidence references. Maps does not copy profiler bundle internals into its public contracts. Browser journey support should be implemented in runtime-profiler and dogfooded by Maps rather than implemented as a one-off Maps profiler.
