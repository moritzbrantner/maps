# Moonlight integration

Moonlight evaluates immutable reference/candidate identities using Maps-normalized semantic observations and neutral runtime-profiler evidence references. MapLibre/Leaflet-specific normalization stays at the Maps edge. Moonlight owns thresholds/classification and must return inconclusive/error rather than success when required evidence is missing or incomparable.
