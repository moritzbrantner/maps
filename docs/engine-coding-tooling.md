# coding-tooling integration

coding-tooling should discover stable engine-scenario capabilities declared by Maps and invoke them without owning scenario semantics. The Maps repository remains the source of truth for scenario ids/fixtures; coding-tooling surfaces availability and deterministic findings. Missing discovery is a tooling gap, not proof that a scenario passed.
