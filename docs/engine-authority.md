# Authority rule

For each map concern, record one current semantic authority and any explicit reference/fallback implementation. Never describe a shadow/reference implementation as authoritative. Authority transfer is an explicit reviewed event backed by parity/evidence and followed by convergence of the superseded path.
