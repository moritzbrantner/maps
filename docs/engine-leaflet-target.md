# Leaflet-class independence target

The first independence landmark is functional, not branding-driven: Maps owns camera/projection/interaction, raster XYZ tile/source/cache lifecycle and application overlay rendering so a useful interactive map works with no MapLibre/Leaflet runtime. Compatibility with every Leaflet plugin or API is not required for this milestone.
